{
  "name": "todo-list-app-server",
  "version": "1.0.0",
  "description": "Backend for Todo List App",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo \"Error: no build specified\" && exit 1",
    "preview": "echo \"Error: no preview specified\" && exit 1",
    "lint": "echo \"Error: no lint specified\" && exit 1",
    "format": "echo \"Error: no format specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
