// Define constants for the backend

const STATUSES = ['Pending', 'Completed'];
const PRIORITIES = ['High', 'Medium', 'Low'];
const RECURRING_OPTIONS = ['None', 'Daily', 'Weekly', 'Monthly'];

module.exports = {
  STATUSES,
  PRIORITIES,
  RECURRING_OPTIONS
};